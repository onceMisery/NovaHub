# NovaHub 项目上下文

## NovaHub

Windows 与 macOS 上的本地优先桌面生产力入口。仓库内统一使用 NovaHub 作为产品名。

## 命令

可被搜索并主动执行的最小能力入口。不要与插件包或菜单 UI 混称。

## 提供器

向主搜索返回候选结果的内置模块，例如应用、文件、计算器和剪贴板。第三方能力称为插件命令。

## 插件包

整体安装、更新、禁用和卸载的 Rust/WASM 版本化单元。不要把插件前端和插件后端视为两个独立生命周期。

## 官方视图

插件返回的受限声明式 UI 模型，由 NovaHub 宿主使用 Slint 渲染。不要称为网页或 DOM。

## 能力

宿主明确授予插件的受控系统操作，例如指定 Origin 的 HTTPS 或用户选择的文件句柄。不要称为无限权限。

## Plugin Host

按需启动、承载 Wasmtime 并执行插件会话的独立进程。不要把它当作插件本身或常驻后台服务。

## 插件会话

用户打开命令到关闭视图之间的短生命周期实例。MVP 不支持插件会话脱离用户调用常驻。
