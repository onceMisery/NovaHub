# NovaHub 方案落地任务计划

> V1 采用能力对标 + 迁移辅助的平衡方案；设计规格与实施计划已落盘，Figma 同步仍受外部配额限制。

## 目标

在本仓库落盘一套以 NovaHub 为唯一产品名的可执行产品与技术方案，覆盖 MVP、插件化前后端、跨 Windows/macOS、性能与内存预算、UI/UX、权限安全、发布运维、路线图、长期展望与最终形态。

本轮范围是设计与方案文档，不初始化实现代码；实现阶段需在设计文档经用户确认后单独规划。

## 阶段

- [x] 1. 检查仓库基线与确认用户决策
- [x] 2. 建立 NovaHub 产品目标、术语和边界
- [x] 3. 完成总体架构、插件契约和生命周期设计
- [x] 4. 完成 UI/UX 设计系统与交互规范
- [x] 5. 完成安全、数据、发布、测试和性能方案
- [x] 6. 完成 MVP 到最终形态的路线图与展望
- [x] 7. 写入文档并运行完整自检
- [ ] 8. 在 Figma 创建 NovaHub 核心桌面原型并验证（Foundations 已创建；组件和屏幕受 Starter MCP 配额限制，完整本地交互原型已验证）
- [ ] 9. 提交文档与原型供用户审阅
- [x] 10. 确认 V1 兼容层级、14 项平衡范围和 Windows 优先策略
- [x] 11. 完成 V1 热门能力、现代化体验与迁移边界设计
- [x] 12. 完成 V1 原型扩展与生产实施计划
- [x] 13. 按实施计划扩展 35 个 V1 确定性原型并完成本地验收
- [ ] 14. 按实施计划进入 Rust/Slint/WIT 生产代码实现

## 已确认决策

- 目标平台：Windows + macOS
- 产品定位：类似 Raycast/uTools 的桌面生产力启动器
- MVP：应用启动、计算器、剪贴板历史、基础文件搜索、系统命令、插件管理、Rust/WASM SDK
- UI：Rust + Slint，插件只能使用官方声明式组件
- 插件：Rust/WASM，WIT 作为唯一协议；TypeScript 延后
- 进程：`novahub-app` + 按需启动的共享 `novahub-plugin-host`
- 后台：第三方插件仅在用户调用时运行，不允许常驻服务或定时任务
- 卸载：默认删除插件拥有的本地代码、数据、凭据、缓存和权限
- V1 兼容：能力对标 + 配置/清单迁移辅助，不运行 uTools/Raycast 插件
- V1 范围：14 项平衡版本；Windows 功能完整优先，macOS 首发核心能力

## 验收

- 文档名称与内容统一使用 NovaHub
- 方案明确所有权、兼容边界、非目标和迁移/退出条件
- UI/UX 规范能直接指导 Slint 组件实现
- 设计自检无 TBD/TODO、矛盾或未说明的关键假设
- Git 工作区仅包含本次方案相关文件，现有内容不被覆盖

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| `ui-ux-pro-max` 脚本路径首次按 `C:\Users\cmira\.agents\src` 解析失败 | 1 | 改按相对路径指针继续定位 |
| 相对路径指向的 `C:\Users\cmira\src\ui-ux-pro-max\scripts` 仍不存在 | 2 | 确认技能包只有路径占位文件，改为基于已读取规则手工生成设计系统 |
| 在用户目录搜索 UI/UX 脚本超时 | 1 | 停止广域搜索，避免重复耗时；采用规则手工综合的替代路径 |
| 首次 Markdown 相对链接扫描使用错误的管道对象作用域，`Join-Path` 收到空路径 | 1 | 保存外层文件目录变量后重跑，不重复原脚本 |
| Figma 账号存在两个计划，创建文件必须由用户指定归属；连续三次续跑均未收到选择 | 3 | 按长期任务协议记录为真实阻塞，收到 `1` 或 `2` 后恢复 |
| Figma Starter 计划限制 3 页、单变量模式且当前变量创建不可用 | 2 | 页面无损压缩为 Foundations / Components / Prototype；令牌以显式 Foundations 文档表示 |
| Figma Starter MCP 调用额度耗尽，后续工具在本轮也未暴露 | 2 | 保留已创建节点，使用仓库 `prototype/` 作为可验证源真值，配额恢复后同步而不重建设计 |
| 批量读取多个大文件时 PowerShell 调用超时，且首次未显式指定 UTF-8 导致显示乱码 | 2 | 改为 `Get-Content -Encoding utf8` 分批读取；确认文件本身未被改写 |
| 首轮交付验证用 `Select-String '^## Task'` 读取整段字符串，未启用多行匹配导致任务数误报 | 1 | 改用 `rg '^## Task [0-9]+'` 按行计数后重跑完整验证 |
| 占位词扫描命中计划自检中“无占位词”的字面声明 | 1 | 改写为不复述扫描关键字的等义表述，保持自动门槛无歧义 |
