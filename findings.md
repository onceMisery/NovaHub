# NovaHub 研究发现

## 仓库基线

- 目标目录：`C:\user-data\code\github\NovaHub`
- 当前是 Git 仓库，分支 `main`，只有初始提交 `5e32f41`
- 现有 `README.md` 只有标题 `NovaHub` 与中文名 `星汇`
- 没有现有代码、`AGENTS.md`、`CONTEXT.md` 或架构文档

## 需求决策

- 首版同时支持 Windows 和 macOS
- 目标是低内存、高响应、UI 稳定的桌面启动器
- 插件前后端可插拔，但首版使用单一 Rust/WASM 插件包
- 插件 UI 为官方声明式组件，不开放任意 HTML/WebView
- 插件只在用户调用时运行
- 卸载默认删除插件所有本地数据
- MVP 需要可直接使用，而不是只有插件平台内核

## 关键架构判断

- 宿主 UI 和第三方代码必须分进程；首选共享、按需启动的 Plugin Host
- WIT 是插件协议唯一所有者，Rust SDK 只是薄封装
- Slint 负责宿主渲染，插件返回受约束的 View 模型
- WASM 本身不是高权限机制，所有系统能力必须经宿主 capability 授权
- 任意 HTML 插件会显著增加内存、隔离和升级成本，列为后续扩展

## UI/UX 技能环境

- `ui-ux-pro-max` 技能目录中的 `scripts` 和 `data` 是相对路径占位文件
- 占位文件指向的实际目录在当前机器不存在，无法运行推荐检索脚本
- 已完整读取技能中的设计、无障碍、性能、动画、表单与主题规则，可手工生成等价设计系统

## Figma 交付

- 用户明确要求在完整方案文档完成后，使用 Figma 插件创建 NovaHub 原型
- 原型至少覆盖启动器、插件声明式视图和插件管理三个核心工作流
- Figma 原型必须与 Slint 官方组件契约、主题令牌和桌面键盘导航一致

## V1 热门能力决策（2026-08-13）

- 用户选择兼容策略 B：原生能力对标 + 配置、清单和数据迁移辅助
- 用户选择平衡范围 B：首期 14 项，而不是 8 项精简或 20+ 项激进覆盖
- Windows 首期功能完整优先；macOS 从首阶段保持同一领域协议并完成核心能力
- 高频、OS 深度、隐私与性能敏感能力归宿主内置 Provider
- 网络型、表单型和纯转换工具归官方 WASM 插件，且不使用隐藏特权 API
- 迁移中心只解析用户显式选择的导出文件，不执行插件/脚本，不迁移凭据、剪贴板或云端数据
- Figma Starter 继续使用三页结构；本地确定性原型是可验证源真值，Figma 是同步镜像
